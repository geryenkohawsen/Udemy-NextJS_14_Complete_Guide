export type DummyData = {
  products: {
    id: string
    title: string
    description: string
  }[]
}

export type LoadedProduct = {
  loadedProduct: {
    id: string
    title: string
    description: string
  }
}
